# Data Prepper Server Configuration
# Controls Data Prepper's runtime behavior and resource usage

# SSL/TLS configuration - disabled for development
ssl: false

# Enable experimental APM plugins
experimental:
  enabled_plugins:
    processor:
      - otel_apm_service_map
    sink:
      - prometheus
